# УСИЛЕННЫЕ ПРАВИЛА ДЛЯ AI ПОМОЩНИКОВ

## 🚨 КРИТИЧЕСКИЕ ТРЕБОВАНИЯ
- АБСОЛЮТНЫЙ ЗАПРЕТ НА ЛОЖЬ И ИМИТАЦИЮ
- Каждая команда должна быть РЕАЛЬНО выполнена через инструменты
- Каждый результат должен быть РЕАЛЬНО проверен
- Никогда не говорить "готово" без реальной проверки
- Принцип: РЕАЛЬНОСТЬ ПРЕВЫШЕ ВСЕГО

## 📋 ОБЯЗАТЕЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ
1. ПРОЧИТАТЬ документацию: README.md, PROJECT_MANIFEST_V2.md, CHAT_SUMMARY.md
2. ПОНЯТЬ архитектуру: REST API, MCP Server, пайплайн обработки
3. ВАЛИДИРОВАТЬ JSON план по PlanInputSchema
4. ПРОВЕРИТЬ пути к файлам (абсолютные, существующие)
5. ТЕСТИРОВАТЬ на малых данных перед большими задачами
6. ПРОВЕРЯТЬ результат (логи, файлы, статус)

## 🔍 ВАЛИДАЦИЯ JSON ПЛАНОВ
- ОБЯЗАТЕЛЬНО использовать правильные поля:
  - `music` (НЕ `backgroundMusic`)
  - `tts: { provider: "kokoro"|"openai", text: "..." }`
  - `transcribeAudio: true`
  - `burnSubtitles: true`
  - `effects: [{ kind: "zoom", params: {...} }]` (массив)
  - `videoOverlays: [{ type: "effect", file: "...", end: 5 }]`

## 🛠 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ
- Всегда использовать абсолютные пути к файлам
- Избегать специальных символов в именах файлов
- Проверять существование файлов перед использованием
- Следовать правильному порядку эффектов (VHS ПЕРЕД оверлеями)
- Логировать полные FFmpeg команды для диагностики

## 🧪 ТЕСТИРОВАНИЕ
- Начинать с простых тестов (1-2 фото)
- Проверять каждый этап пайплайна
- Анализировать логи при ошибках
- Проверять размер и качество итогового видео
- Тестировать все функции отдельно перед комбинированием

## 📊 ДИАГНОСТИКА ПРОБЛЕМ
- При ошибках FFmpeg: проверить filter_complex и индексы
- При отсутствии аудио: проверить music/tts поля
- При отсутствии субтитров: проверить transcribeAudio/burnSubtitles
- При больших размерах: проверить CRF и разрешение
- При ошибках валидации: сверить с PlanInputSchema

## 🚫 ЗАПРЕЩЕННЫЕ ДЕЙСТВИЯ
- НЕ редактировать dist/ (только src/)
- НЕ добавлять зависимости без запроса
- НЕ менять публичные API без одобрения
- НЕ удалять файлы автоматически
- НЕ говорить "готово" без проверки
- НЕ игнорировать документацию
- НЕ использовать неправильные поля JSON

## ✅ КРИТЕРИИ УСПЕХА
- Все тесты проходят (9/10 как минимум)
- Видео создается с правильным размером
- Аудио и субтитры работают корректно
- Эффекты накладываются в правильном порядке
- Логи не содержат ошибок
- Результат доступен для скачивания

## 📝 ОТЧЕТНОСТЬ
- Обязательно показывать diff изменений
- Предоставлять точные команды для запуска
- Указывать пути к результатам
- Анализировать логи при ошибках
- Давать рекомендации по улучшению

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ
- ESM/NodeNext: импорты в рантайме с явными .js расширениями
- При TS-ошибках (TS2307/TS2339) — указать точные файлы/строки и предложить минимальный фикс
- Видео-оверлеи = эффекты, не контент. Накладывать через ffmpeg blend
- Несколько оверлеев — цепочкой через tmp-метки в filter_complex

## 🎯 ПОШАГОВЫЙ КОНТУР РАБОТЫ
S0: Прочитать MANIFEST/DEV_PROMPT.md и перечисли риски/долги
S1: Чистая сборка: `npm run build`. Если падает — приоритизировать типовые ошибки TS
S2: Валидация сценария JSON: проверить наличие файлов, времена start/end
S3: Быстрый прокси-рендер базового таймлайна → out/proxy_base.mp4
S4: Быстрый прокси-рендер с оверлеями → out/proxy_overlays.mp4
S5: Кадровая проверка (QA): извлечь кадры, сравнить ожидаемые зоны
S6: Whisper-сабтайтлы: сгенерировать .srt/.ass, наложить в прокси-рендер
S7: TTS (Kokoro): сгенерировать дорожку, замиксовать с музыкой (ducking)
S8: Полный рендер (полное качество) → out/final.mp4. Только когда S0–S7 зелёные