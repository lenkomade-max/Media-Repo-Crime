/**
 * SubtitleRenderer — устаревший модуль.
 * В Этапе 2 субтитры собираются через Whisper + SubtitlesStyler.
 * Этот файл оставлен как заглушка, чтобы не ломать сборку.
 */
export function renderSubtitles(): void {
  throw new Error("SubtitleRenderer is deprecated. Use SubtitlesStyler + Whisper instead.");
}
